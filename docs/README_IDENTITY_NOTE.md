# Project identity note

The canonical project name is **CMIS — Cross-Chain Market Intelligence Service**.

The existing `liquidity_scout` Python namespace and historical repository slug are retained temporarily for compatibility during the staged identity migration. They should not be interpreted as the long-term architectural project name.
